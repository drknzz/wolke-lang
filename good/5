# (funkcje lub procedury, rekurencja)

def fib(n: Int) -> Int {
    if n == 0 or n == 1 {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

def nothing() -> Void {
    pass;
    return;
}
