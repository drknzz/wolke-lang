# (three types)
# (literals, arithmetic, comparison)
# (variables, assignment)

def main() -> Void:
    Int x = (2 + 3 * 4 / 5) % 6
    Int y = (6 + 5 * 4 / 3) % 2
    Boolean z = x >= y
    assert(z)
    assert("a" == "a")